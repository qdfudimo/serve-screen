const {
  getData
} = require("../api")
const {
  formatTime
} = require("./moment")
const fs = require('fs')
const path = require('path')
/**
 * 判断文件是否存在
 * @param fileUrl 文件路径
 * @returns 疫情数据
 */
async function judgeFileData() {
}
module.exports = {
  judgeFileData
}